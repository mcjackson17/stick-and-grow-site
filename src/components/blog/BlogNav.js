// The blog pages predate the shared site chrome. Rather than restructure them,
// BlogNav now renders the same SiteNav every marketing page uses, so the
// navigation stays consistent site-wide from one definition.
export { default } from '../SiteNav'
